name = "telegram-osint-bot"
main = "worker.js"
compatibility_date = "2024-01-01"
compatibility_flags = ["nodejs_compat"]

# No static assets
[site]
bucket = ""

# Deploy from this directory:
#   cd relay && npx wrangler deploy
#
# Or from project root (uses root wrangler.toml):
#   npx wrangler deploy
